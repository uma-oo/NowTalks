-- database: /home/aayoubst/Desktop/real-time-forum/backend/database/forum.db

	SELECT
		s.nickname AS sender,
		r.nickname AS receiver,
		messages.message,
		messages.createdAt,
		messages.messageID
	FROM
		messages INNER JOIN users s
		ON messages.senderID = s.userID 
		JOIN users r ON 
		messages.receiverID = r.userID
	WHERE
		senderID IN (3, 4)
		AND receiverID IN (3, 4)
		AND messages.messageID < 27
	ORDER BY  messages.createdAt DESC
	LIMIT
		10